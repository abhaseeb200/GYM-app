1. login mei. agir value fill kerne bad dobra emppty kerte hai tw wo already wala alert dyta hai jo ke nahi anna chahie (its means k validation pass nahi howi hai to submit button nahi chalna chahie)
2. register mei. agir value fill kerne bad submit pe click our error aie validation wala. phr agir username or email ki value empty bhi hotti hai to tbhi 2nd time pe wo submit pas hojata hai jo nahi honna chahie
3. create components for navbar & other if required
4. workout page mei intial wala select nahi honna chahaie.
5. show order number in workout page.

BUGS

Register/Login
- email start should be start from alphabets
- password combination should be show seperate errors
- handlePassword(). code should be not excute next to it, if password field are empty (code)
- handleUsername(). same from password (code)
- handleInput(). Next code will not be excute if they found same username (code).

Home
- In 'add excercise' button if user close the tab fields values should be empty
- Empty value should not be add. add validation process
- In 'Start working' button change to myworkout.html
- Loop should be break if found same excercise name

Navbar
- Display workout count in barket with the username

Workout
- First option from select should be disabled or remove.
- Initial option will also work onchange event. same as 4
- Sweet alert show after save workout

My Workout
- My workout table should be display as ascending order
- 'myStartWorkout' move to currentlyLogin

Excercise Start
- Display 'myStartWorkout' data, and add flag if they are complete their excercise
- Add flag to 'myStartWorkout' in localStorage

